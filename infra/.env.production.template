# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# 
# SECURITY WARNING: This file contains placeholder values for production deployment.
# Copy this file to .env and replace ALL placeholder values with actual secrets.
# NEVER commit the actual .env file to version control.
#
# Generate secure passwords with: openssl rand -base64 32
# Generate UUIDs with: uuidgen
# Generate JWT secrets with: openssl rand -hex 64

# =============================================================================
# CRITICAL SECURITY CREDENTIALS (REQUIRED - NO DEFAULTS)
# =============================================================================

# HashiCorp Vault Configuration
VAULT_ROOT_TOKEN=REPLACE_WITH_SECURE_TOKEN_GENERATED_BY_OPENSSL_RAND_HEX_64
VAULT_TOKEN=REPLACE_WITH_VAULT_ACCESS_TOKEN_FROM_VAULT_INIT
VAULT_TLS_DISABLE=false

# Redis Authentication (CRITICAL - REQUIRED)
REDIS_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_OPENSSL_RAND_BASE64_32
REDIS_MAX_MEMORY=2gb

# RabbitMQ Authentication (CRITICAL - REQUIRED)
RABBITMQ_USER=admin_user
RABBITMQ_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_OPENSSL_RAND_BASE64_32

# =============================================================================
# SMS SERVICE CREDENTIALS (REQUIRED FOR SMS FUNCTIONALITY)
# =============================================================================

# Twilio Configuration (Primary SMS Provider)
TWILIO_ACCOUNT_SID=your_twilio_account_sid_from_console
TWILIO_AUTH_TOKEN=your_twilio_auth_token_from_console
TWILIO_PHONE_NUMBER=+15551234567
TWILIO_AREA_CODE=555

# AWS SNS Configuration (Backup SMS Provider)
AWS_SNS_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
AWS_SNS_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_SNS_REGION=us-east-1

# =============================================================================
# EMAIL AUTOMATION CREDENTIALS (REQUIRED FOR EMAIL SERVICES)
# =============================================================================

# Email Service API Keys
RAPIDAPI_KEY=your_rapidapi_key_from_rapidapi_dashboard
TWOCAPTCHA_API_KEY=your_2captcha_api_key
ANTICAPTCHA_API_KEY=your_anticaptcha_api_key
CAPMONSTER_API_KEY=your_capmonster_cloud_api_key

# =============================================================================
# MONITORING & OBSERVABILITY
# =============================================================================

# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_OPENSSL_RAND_BASE64_32

# Kibana Configuration
KIBANA_PASSWORD=REPLACE_WITH_SECURE_PASSWORD_OPENSSL_RAND_BASE64_32

# Alert Webhooks
ALERT_WEBHOOK_URL=https://your-monitoring-system.com/webhooks/alerts
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# =============================================================================
# INFRASTRUCTURE & CLOUD CREDENTIALS
# =============================================================================

# Cloud Provider Configuration
CLOUD_API_TOKEN=your_cloud_provider_api_token
GCP_PROJECT=your-gcp-project-id
HETZNER_API_TOKEN=your_hetzner_cloud_api_token

# Proxy Configuration
BRIGHTDATA_PROXY_URL=http://username:password@proxy.brightdata.com:port

# Chat/Bot Integration
CHAT_API_TOKEN=your_telegram_or_discord_bot_token

# =============================================================================
# APPLICATION SECURITY SETTINGS
# =============================================================================

# JWT and API Security
JWT_SECRET=REPLACE_WITH_SECURE_JWT_SECRET_OPENSSL_RAND_HEX_64
SMS_SERVICE_API_KEY=REPLACE_WITH_SECURE_API_KEY_OPENSSL_RAND_HEX_32
EMAIL_SERVICE_API_KEY=REPLACE_WITH_SECURE_API_KEY_OPENSSL_RAND_HEX_32

# Session Security
SESSION_SECRET=REPLACE_WITH_SECURE_SESSION_SECRET_OPENSSL_RAND_HEX_64
ENCRYPTION_KEY=REPLACE_WITH_32_BYTE_ENCRYPTION_KEY_OPENSSL_RAND_HEX_32

# =============================================================================
# PERFORMANCE & SCALING CONFIGURATION
# =============================================================================

# Worker Configuration
WORKER_COUNT=5
SMS_WORKER_CONCURRENCY=8
SMS_WORKER_PREFETCH=20
SMS_WORKER_REPLICAS=3

# Queue Configuration
MESSAGE_TTL_SECONDS=7200
MAX_RETRIES=5
RABBITMQ_PREFETCH_COUNT=20

# Connection Pool Settings
REDIS_MAX_CONNECTIONS=200
RABBITMQ_MAX_CONNECTIONS=2000
RABBITMQ_MAX_CHANNELS_PER_CONNECTION=200

# Memory Configuration
RABBITMQ_VM_MEMORY_HIGH_WATERMARK=0.8
RABBITMQ_DISK_FREE_LIMIT=5GB

# =============================================================================
# CIRCUIT BREAKER & RELIABILITY
# =============================================================================

# Circuit Breaker Settings
CIRCUIT_BREAKER_THRESHOLD=15
CIRCUIT_BREAKER_TIMEOUT=60

# Rate Limiting
RATE_LIMIT_PER_PHONE=10
RATE_LIMIT_WINDOW=300
RATE_LIMIT_PER_MIN=200

# Retry Configuration
HTTP_TIMEOUT=30
HTTP_MAX_RETRIES=3
HTTP_BACKOFF_FACTOR=2

# =============================================================================
# TLS/SSL CONFIGURATION
# =============================================================================

# TLS Settings (All services use TLS in production)
REDIS_TLS_ENABLED=true
RABBITMQ_TLS_ENABLED=true
VAULT_TLS_ENABLED=true

# Certificate Paths (mounted in containers)
TLS_CERT_PATH=/tls/server.crt
TLS_KEY_PATH=/tls/server.key
TLS_CA_PATH=/tls/ca.crt

# =============================================================================
# ENVIRONMENT FLAGS
# =============================================================================

# Environment Configuration
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
METRICS_ENABLED=true

# Feature Flags
ENABLE_TLS=true
ENABLE_METRICS=true
ENABLE_TRACING=true
ENABLE_HEALTH_CHECKS=true

# =============================================================================
# VALIDATION CHECKLIST
# =============================================================================
#
# Before deploying, verify:
# □ All REPLACE_WITH_* values have been replaced with actual secrets
# □ All API keys are valid and have necessary permissions
# □ TLS certificates are generated and placed in config/tls/
# □ Vault is initialized and unsealed
# □ All services can connect to Redis with authentication
# □ RabbitMQ management UI is accessible with new credentials
# □ No development tokens remain in any configuration files
# □ All webhooks and monitoring endpoints are configured
# □ Security scan shows 0 critical vulnerabilities
# □ Backup and disaster recovery procedures are in place
#
# =============================================================================
# SECURITY REMINDERS
# =============================================================================
#
# 1. NEVER commit this file to version control after adding real values
# 2. Use environment-specific secret management (AWS Secrets Manager, etc.)
# 3. Rotate all credentials regularly (30-90 day cycles)
# 4. Monitor for credential exposure in logs and error messages
# 5. Implement principle of least privilege for all service accounts
# 6. Enable MFA where supported by service providers
# 7. Regular security audits of all integrated services
# 8. Monitor unusual API usage patterns
#
# =============================================================================

# Example secure values (DO NOT USE THESE IN PRODUCTION):
# REDIS_PASSWORD=Kj8mN2pQr9sT7vXz3A6fG1hL4nB8cE5m
# RABBITMQ_PASSWORD=P3kR7mN9wQ2xS5vZ8A1fD4gH6jL9nM7c
# JWT_SECRET=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456